GEMINI_API_KEY="your_gemini_api_key_here"

# Optional. Defaults to the OS temp directory so uvicorn --reload does not
# restart while audio uploads and chunks are being written.
# MEDIA_DIR="D:/PodC/media"

# Optional for deployed frontends.
# CORS_ORIGINS="https://your-frontend-domain.example"
